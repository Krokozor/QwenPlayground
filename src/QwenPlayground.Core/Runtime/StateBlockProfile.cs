namespace QwenPlayground.Core.Runtime;

/// <summary>
/// Кусок профиля чата «state-блок» — служебная информация в начале мысли (msg_id, время,
/// контекст, сборка, nag самопроверки). Это НЕ системный промпт, поэтому настройки
/// выделены отдельно: специализированному чату может быть нужен свой промпт, но шумный
/// агентный state-блок — ни к чему. Сегодня гранулярность одна — вкл/выкл; новые поля
/// добавляются по мере появления реальных потребностей, впрок не плодим.
/// </summary>
public sealed class StateBlockProfile
{
    /// <summary>Вставлять state-блок в ходы этого чата. false — блоков нет вовсе.</summary>
    public bool Enabled { get; set; } = true;
}
